<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.smhrd.foodie.mapper.RecipeInfoMapper">
    <!-- 조회수가 높은 상위 5개 레시피를 가져오는 쿼리 -->
    <!-- <select id="findTop5ByOrderByRecipeViewsCntDesc" resultType="com.smhrd.foodie.model.RecipeInfo">
        SELECT * FROM recipe_info ORDER BY recipe_views_cnt DESC LIMIT 5
    </select> -->

    <!-- 가장 최근에 등록된 상위 5개 레시피를 가져오는 쿼리
    <select id="findTop5ByOrderByIdDesc" resultType="com.smhrd.foodie.model.RecipeInfo">
        SELECT * FROM recipe_info ORDER BY id DESC LIMIT 5
    </select> -->

    <!-- 랜덤으로 5개의 레시피를 가져오는 쿼리
    <select id="findRandomRecipes" resultType="com.smhrd.foodie.model.RecipeInfo">
        SELECT * FROM recipe_info ORDER BY RAND() LIMIT 5
    </select>  -->
</mapper>
